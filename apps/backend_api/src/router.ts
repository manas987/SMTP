import { Router } from "express";

export const router = Router();

router.use("/auth",);

router.use("/org", () => {});

router.get("/list", () => {});

router.post("/email", () => {});

router.post("/emails", () => {});
